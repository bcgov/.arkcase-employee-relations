application:
 name: 
  active: arkcase,arkcase-activemq,ldap
  default: arkcase
 profile: runtime
configuration:
 server:
  url: http://localhost:9999
  update.path: "/config/{applicationName}"
  remove.path: "/config/remove/{applicationName}"
  reset.path: "/reset"
  update.file.path: "/file"
  modules.path: "/config/modules"
  #username: root
  #password: root